

    spring:
        config:
            activate:
                on-profile: prd

        datasource:
            url: jdbc:postgresql:shortline.proxy.rlwy.net:46851/railway
            username: postgres
            password: uAmpLtAKBqVjkoTTzbVLtQYCfcwpdgnS
            driver-class-name: org.postgresql.Driver

        jpa:
            open-in-view: false
            hibernate: 
                ddl-auto: update
            show-sql: true
            properties:
                hibernate:
                    dialect: org.hibernate.dialect.PostgreSQLDialect
                    format-sql: true
        
    server:
        port: ${PORT:8080}